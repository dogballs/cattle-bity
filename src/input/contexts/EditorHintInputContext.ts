import { InputControl } from '../InputControl';
import { InputContext } from '../InputContext';

export const EditorHintInputContext: InputContext = {
  Skip: [InputControl.Select, InputControl.PrimaryAction],
};
